{
  "name": "coc-iiot-talk-2026-presentation",
  "version": "1.0.0",
  "description": "Reveal.js Docker setup for creating and presenting AsciiDoc-based presentations",
  "main": "scripts/watch.js",
  "scripts": {
    "build": "node scripts/compile.js",
    "dev": "node scripts/watch.js",
    "test:python": "python -m unittest discover -s tests",
    "test:js": "jest",
    "test": "npm run test:python && npm run test:js"
  },
  "dependencies": {
    "@asciidoctor/core": "^3.0.0",
    "@asciidoctor/reveal.js": "^5.0.0",
    "chokidar": "^3.6.0",
    "fs-extra": "^11.2.0",
    "live-server": "^1.2.2",
    "reveal.js": "^5.1.0"
  },
  "devDependencies": {
    "jest": "^29.0.0",
    "puppeteer": "^22.0.0"
  }
}
